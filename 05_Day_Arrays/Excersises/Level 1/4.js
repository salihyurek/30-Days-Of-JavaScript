let countries = Array(6)
countries = ['Turkey', 'United States', 'Argentina', 'Espana', 'Germany', 'Italy']

let firstItem = countries[0]
let middleItem = countries[Math.floor(countries.length / 2)]
let lastItem = countries[countries.length - 1]

console.log('first item', firstItem, '\nmiddle item', middleItem, '\nlast item', lastItem)